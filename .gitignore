# Секреты и переменные окружения
.env

# Ключи сервисных аккаунтов Google.
# Маской, а не именем файла: имя ключа раскрывает id проекта Google,
# а .gitignore лежит в публичном репозитории.
*.json

# Сессия Telegram (Telethon)
*.session
*.session-journal

# Python
__pycache__/
*.pyc
